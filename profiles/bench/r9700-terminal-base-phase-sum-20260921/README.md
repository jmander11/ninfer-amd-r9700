# Corrected phase-reporting terminal base campaign

Create-only successor to the stopped FP8-context whole campaign. No old package is resumed.
Reopen its resource bridge, published chunk2048 and quality authorities, and all48 validated
capacity reports (32 original nonhybrid plus16 accounted hybrid). Preserve all12 candidates and
both mixed-XAttention numerical exclusions. Run only the ten eligible whole matrices at C1..4,
then all-repetition graph versus one eager public-token control at each eligible cell.

Root owns physical execution and the separate `build.sh`; this owner never builds or reconverts.
After all four phase-sum builds and the reviewed reporter-only source record exist:

```sh
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh host-checks
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh freeze --review /absolute/path/to/reporter-source-review.json
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh preflight
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh whole
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh controls
bash profiles/bench/r9700-terminal-base-phase-sum-20260921/commands.sh select
```

`host-checks` runs only the four `--host-split512-routing` CPU checks and binds stdout/stderr.
`freeze` publishes `reporting-recovery.json` only after the common bridge validator accepts it;
`preflight` is read-only. Whole execution requires corrected schema21 reports after every matrix.
All output roots are exclusive: any interrupted or failed attempt is retained, never resumed.
`select` replays exact controls before assembling all12 candidates with both resource/reporting
bridges, old capacity, and new whole reports. Schema7's24 phase objectives are unchanged; no old
concurrent prefill maximum is reinterpreted as a sum. Old C1/C2 partial whole reports remain diagnostic
history only. No new numerical, capacity, or kernel qualification campaign is created here.

The retained `reporter-smoke/report.json` is the completed C2 P128+G2/schema21 integration check
(chunk2048, spec-none, one repetition/no warmup, two lanes with three retained tokens each).
The native reader accepted the corrected semantics; auto was checked before/after. This short run
is timing-ineligible and is not a whole-campaign measurement or a reason to repeat qualification.
